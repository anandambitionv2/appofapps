kind: Service
apiVersion: v1
metadata:
  name: student-frontend-svc
  namespace: default

spec:
  type: LoadBalancer
  selector:
    app: student-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80